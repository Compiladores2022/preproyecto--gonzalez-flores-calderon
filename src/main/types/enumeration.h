#ifndef ENUMERATION_H
#define ENUMERATION_H

typedef enum {TYPEINT, TYPEBOOL, UNDEFINED, TYPEVOID} types;


typedef enum {METHOD, CONST, VAR, TYPELESS, NEXT, RETURN, METHODCALL} identifierType;

#endif